# Azure Deployment Configuration
# This file is sourced by deployment scripts to configure Azure resources

# Required: Environment prefix (dev, staging, prod)
ENVIRONMENT_PREFIX="dev"

# Required: Project prefix for resource naming
PROJECT_PREFIX="bs"

# Required: Azure region for resource deployment
PROJECT_LOCATION="East US"

# Required: Log folder for deployment logs
LOG_FOLDER="${LOG_FOLDER:-${HOME}/logs}"
mkdir -p "$LOG_FOLDER"

# Required: Azure Resource Group name
PROJECT_RESOURCE_GROUP="DevOps"

# Required: Azure Subscription ID
PROJECT_SUBSCRIPTION_ID="095f691e-d2c6-4e2a-a077-aa223458e150"

# Required: Azure Container Registry name
ACR_NAME="${ENVIRONMENT_PREFIX}${PROJECT_PREFIX}contregistry"

# Optional: Git repository URL (defaults to hk-biashara-os-dev if not set)
REPOSITORY_URL="${REPOSITORY_URL:-https://github.com/dev-personal-projects/hk-biashara-os-dev}"

# Optional: Git branch to deploy (defaults to 'dev' if not set)
GIT_BRANCH="${GIT_BRANCH:-dev}"

# Optional: Target port for container app (defaults to 8080 if not set)
TARGET_PORT="${TARGET_PORT:-8080}"

# Optional: Service Principal credentials for container registry authentication
# If not provided, the script will automatically create a new Service Principal
# and store its credentials in GitHub Secrets (if GitHub CLI is authenticated)
# SERVICE_PRINCIPAL_CLIENT_ID="your-client-id"
# SERVICE_PRINCIPAL_CLIENT_SECRET="your-client-secret"
# SERVICE_PRINCIPAL_TENANT_ID="your-tenant-id"

# Optional: Key Vault name (defaults to ${ENVIRONMENT_PREFIX}-${PROJECT_PREFIX}-kv if not set)
# Use this if your Key Vault has a different name (e.g., dev-bos-kv instead of dev-bs-kv)
# KEY_VAULT_NAME="dev-bos-kv"
